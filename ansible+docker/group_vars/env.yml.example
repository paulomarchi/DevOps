env: staging # env name, this will be used as an inventory file name
app_code_user: centos # remote user
aws_region: sa-east-1 # AWS region, where instance will be created
instance_type: t2.medium  # AWS instance type
ami: ami-xxxx  # AMI id
project_name: QA_Testing
